import React from 'react';
import preloader from '../../../img/Prelouder.gif';

const Preloader = () => {
    return (
        <div>
            <img src={preloader}/>
        </div>
    );
};

export default Preloader;